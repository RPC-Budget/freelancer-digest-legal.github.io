services:

  docs:

    build: .

    container_name: freelancer-digest-legal

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    command: >
      mkdocs serve
      --dev-addr=0.0.0.0:8000